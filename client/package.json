{
  "name": "rxmarble",
  "private": true,
  "scripts": {
    "build:cli": "parcel build -t node cli/dist.ts",
    "build:copy": "node dist/dist.js",
    "serve": "parcel serve packages/samples/index.html",
    "build": "parcel build index.html --public-url . "
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.1",
    "fs-extra": "^9.0.1",
    "lerna": "^3.22.1",
    "npx-run": "^2.1.2",
    "parcel-bundler": "^1.12.4",
    "typescript": "^4.0.2"
  },
  "dependencies": {},
  "publishConfig": { "registry": "https://npm.pkg.github.com/" }
}
